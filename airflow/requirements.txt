requests
pyarrow>=13
duckdb>=1.0
pandas
great-expectations>=1.9
# Avoid installing Airflow providers at runtime to prevent downgrading Airflow core
# dbt-common (<0.7) requires isodate<0.7; pin to avoid resolver backtracking to 0.7.2
isodate==0.6.1
# Upgrade dbt to 1.8.x to align with newer urllib3 & pathspec constraints in Airflow 3.1
dbt-core==1.8.4
dbt-duckdb==1.8.2
# Align with Airflow constraints (currently requesting openlineage-integration-common==1.39.0)
openlineage-dbt==1.39.0
